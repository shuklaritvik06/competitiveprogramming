#include <iostream>
using namespace std;
int main() {

  // Use can use merge sort approach

  // suppose we get 2 sorted arrays from recursion
  // [1,4,6] and [2,3,7]
  /*
    first we will compare 1 and 2
    add 1 in array then compare 4 and 2
    now 2 is bigger
    so number of inversions is 4 and the rest of the elements after 4 in the
    first array
    and so on
  */

  return 0;
}
